import java.util.*;
public class hashset {//**Note: In the example above, even though BMW is added twice it only 
    //appears once in the set because every item in a set has to be unique.
   //**The HashSet class has many useful methods. For example, to add items to it, use the add() method:



    
    public static void main(String[] args) {
        
        HashSet<String> a=new HashSet<>();
      
        
        /*a.add("volvo");// is difine index a[0]
        a.add("bmw");//a[1]
        a.add("ford");//a[2]
        a.add("bmw");//a[3]
        a.add("mazda");//a[4]*/
        //System.out.println(a.size());
        //a.clear();
        //a.remove("volvo");
      
        System.out.println(a);
        //System.out.println(a.contains("bmw"));
        //System.out.println();
        // for(String i: a){

        //     System.out.println(i);
        // }
            // Create a HashSet object called numbers
   /* HashSet<Integer> numbers = new HashSet<Integer>();

    // Add values to the set
    numbers.add(4);
    numbers.add(7);
    numbers.add(8);

    // Show which numbers between 1 and 10 are in the set
    for(int i = 1; i <= 10; i++) {
      if(numbers.contains(i)) {
        System.out.println(i + " was found in the set.");
      } else {
        System.out.println(i + " was not found in the set.");

}*/
}
    }